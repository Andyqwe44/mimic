---
description: monitor_web 前端 — Tooltip、design tokens、i18n、className 规则
globs: monitor_web/**/*.ts,monitor_web/**/*.tsx,monitor_web/**/*.css
alwaysApply: false
---

# monitor_web

## Tooltip

禁止原生 `title=`。一律：

```tsx
<Tooltip text={t('some.key')}>
  <button>...</button>
</Tooltip>
```

## i18n

- 词典：`locales/{en,zh-CN,zh-TW}.json`；入口 `lib/i18n.ts`；持久化 `settings.json` 的 `locale`（经 `set_setting`，非 localStorage）。
- 插值：**`{var}`**（`i18n.ts` 已设 `prefix:'{'` / `suffix:'}'`）。勿写 `{{var}}`，否则会原样显示花括号。
- `constants.ts` 里 `STATE_LABEL` / `CAPTURE_MODES.label|desc` / 鼠标键盘 `name` 等存的是 **key**，渲染必须 `t(...)`。
- 专有名词/缩写（WGC、DXGI、TCP、PostMessage、SendInput、Game Agent Monitor…）可留英文；其余用户可见文案走目标语言。
- 桌面目标：C++ 仍返回 `" Entire Desktop"`；UI 用 `displayTargetTitle()`（`lib/windowTitle.ts`）本地化显示。

```tsx
// ✅
{t(STATE_LABEL[winState] || winState)}
{t(m.label)}  /  text={t(m.desc)}
{t('settings.version', { version: appVersion })}
// ❌ 直接渲染 key / 漏传插值参数
{STATE_LABEL[winState]}
{t('settings.version')}
```

参考：`MonitorView` / `SettingsView` 的 `t()` 用法。

## 设计令牌 — `src/lib/design.ts`

可复用尺寸/间距/字号/圆角必须来自 `design.ts`（`H` `GAP` `TEXT` `RADIUS` `MODAL_W` `BTN_SIZE_CLASS`…）。禁止在新组件里散落 magic Tailwind。

**ActionBtn 自动宽度**（`btnAutoSize`）：按**视觉单位**计宽——拉丁字母=1，CJK/全角≈2。阈值：≤3→xs，≤6→sm，≤7→md，≤14→lg，否则 xl。按钮加 `whitespace-nowrap`，避免中文四字标签（如「检查更新」）被挤成两行。

## className 禁止静默覆盖

外部 `className` **不得**覆盖组件内部的 `h-*`/`w-*`/`px-*`/`text-*`/`bg-*` 等原子类（Tailwind 声明顺序不确定）。

- 尺寸 → 组件 `size` prop（如 `ActionBtn`）
- 颜色 → `variant` prop
- `className` 仅布局：`flex-1` `shrink-0` `self-center`

## Bridge

`hostCall` 已 unwrap `{id, result}`；SharedBuffer 用 `e.additionalData`（对象，非 `getAdditionalData()`）。
