{
  "name": "mimic-signaling",
  "version": "0.2.4",
  "private": true,
  "description": "Mimic signaling server — account, devices, session mutex (no media relay)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:lan": "node server.js --host 0.0.0.0 --port 8443"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
